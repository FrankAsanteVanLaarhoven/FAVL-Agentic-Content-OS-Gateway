# Offline test dependencies. Installed into a repository-local .venv by
# `make test`; the host base environment is not part of the reproducibility
# contract.
pydantic==2.11.7
sqlalchemy[asyncio]==2.0.43
nats-py==2.11.0
prometheus-client==0.22.1
httpx==0.28.1
pytest==8.4.1
pytest-asyncio==1.1.0
ruff==0.13.1
mypy==1.18.2
types-requests==2.32.4.20250913

# Runtime dependencies of the services, required for type-checking them.
# Without fastapi installed, mypy treats every route decorator as untyped and
# the strict-mode errors are noise rather than signal.
fastapi==0.116.1
asyncpg==0.30.0
alembic==1.16.4
